# datasets are too large for git (COCO-indoor ~15GB); see README for preparation
datasets/

# training outputs: regenerable and large (plots/metrics/weights) -> keep ignored
# (历史结果已存档于 git 历史; 指标汇总改由 tools/model_stats.py 导出)
runs/
# SLURM stdout/stderr logs are huge and regenerable -> keep ignored
logs/
vis_out/

# caches & python artifacts
*.cache
__pycache__/
*.pyc
*.onnx
*.engine

# temp
*.tmp
*.md