# datasets are too large for git (COCO-indoor ~15GB); see README for preparation
datasets/

# training outputs: runs/ is tracked (metrics/plots in git, weights via Git LFS)
# SLURM stdout/stderr logs are huge and regenerable -> keep ignored
logs/
vis_out/

# caches & python artifacts
*.cache
__pycache__/
*.pyc
*.onnx
*.engine

# temp
*.tmp
